[gcode_macro M140]
rename_existing: M99140
gcode:
    #Parameters
    {% set s = params.S|float %}

    M99140 {% for p in params %}{'%s%s' % (p, params[p])}{% endfor %}   ; Set bed temp
    {% if s > 0 %}
        STATUS_HEATING
    {% endif %}
